# Backend Test Map

This document maps backend feature areas to the tests that protect them. Use it to decide which focused tests to run before running the full backend suite.

## How to Run

Default automated backend suite:

```bash
cd backend
uv run --extra test --extra local-ml --extra local-services pytest -q
```

Real external Qwen/DashScope checks are opt-in:

```bash
cd backend
RUN_EXTERNAL_QWEN_TESTS=1 uv run --extra test --extra local-ml --extra local-services pytest tests/integration/test_external_url_clone.py -s -v
```

## Test Database and Isolation

- Automated tests use the in-memory SQLite database configured in `tests/conftest.py`: `sqlite:///:memory:`.
- Automated tests do not use `backend/voice_clone.db`.
- API tests must use the pytest `client` fixture so FastAPI `get_db` is overridden to the test session.
- Do not create module-level `TestClient(app)` instances in tests; they can bypass test database isolation.
- Mock TTS services according to the current contracts: Qwen/CosyVoice synthesis returns an audio file path; Edge-TTS returns `(audio_bytes, "mp3")`.
- Default storage mode is `frontend`. Tests that assert backend history rows, backend audio URLs, or persisted SRT/audio files must explicitly call:

```python
set_storage_mode(db_session, "backend")
db_session.commit()
```

## Directory Structure

| Path | Purpose |
|---|---|
| `tests/unit/` | Unit tests for services and models. |
| `tests/integration/` | API/integration tests using FastAPI fixtures. |
| `tests/fixtures/` | Factory and pytest helper code only. |
| `tests/manual/` | Manual verification scripts only; files must be named `manual_*.py` and are ignored by pytest. |
| `tests/conftest.py` | Shared fixtures, in-memory database setup, API client override, and mocks. |

## Feature-to-Test Mapping

| Feature area | Main code | Tests | Notes |
|---|---|---|---|
| Voice upload, list, detail, delete | `app/api/clone.py`, `app/models/voice_profile.py`, `app/schemas/voice_profile.py` (`VoiceProfileOut`), `app/api/_voice_helpers.py` (`voice_to_dict`) | `tests/integration/test_clone_api.py`, `tests/test_api_clone.py`, `tests/unit/test_voice_profile_model.py` | Uses test DB and temp audio files. A5 fix: `/upload` + `/upload-from-url` return full `voice_to_dict` shape (not trimmed dicts); `response_model=VoiceProfileOut` on upload/upload-from-url/create-clone/create-clone-mimo/create-clone-voxcpm/create-from-design/get/list. |
| Qwen/CosyVoice clone registration | `app/api/clone.py`, `app/services/qwen_tts_service.py` | `tests/integration/test_clone_api.py`, `tests/unit/test_qwen_tts_service.py` | Automated tests mock Qwen. Real Qwen calls are external tests only. |
| External audio URL behavior | GitHub/Qwen URL checks | `tests/integration/test_external_url_clone.py` | Marked `external`; requires `RUN_EXTERNAL_QWEN_TESTS=1` for real Qwen calls. |
| Qwen/CosyVoice TTS API | `app/api/tts.py`, `app/services/qwen_tts_service.py`, `app/schemas/tts.py` (`TTSResultOut`) | `tests/integration/test_tts_api.py`, `tests/test_api_tts.py`, `tests/unit/test_qwen_tts_service.py` | Current contract: Qwen/CosyVoice synthesis returns an audio file path. B-P1-8b: `response_model=TTSResultOut` on `/synthesize`; `/history` -> `{items: [TTSResultOut]}`. |
| Edge-TTS | `app/api/tts.py`, `app/services/edge_tts_service.py`, `app/services/edge_tts_protocol.py`（纯协议：GEC/SSML/帧重组）, `app/services/edge_tts_ws_client.py`（workers 传输层） | `tests/test_api_tts.py`, `tests/unit/test_edge_tts_service.py`, `tests/unit/test_edge_tts_service_strategy.py`（deploy_target 策略分发 + workers 子进程 import 审计）, `tests/unit/test_edge_tts_protocol.py`, `tests/unit/test_edge_tts_ws_client.py`, `tests/test_synthesize_speech_internal.py` | Current mock contract: Edge-TTS returns `(audio_bytes, "mp3")`. workers 模式走内置 WS 客户端（无 edge-tts 包），list_voices 走 voices/list REST（MockTransport 注入）。 |
| MiMo TTS | `app/api/mimo_tts.py`, `app/services/mimo_tts_service.py`（httpx 同步客户端，transport 可注入）, `app/schemas/tts.py` (`TTSResultOut`) | `tests/unit/test_mimo_tts_service.py`（MockTransport：请求形状/HTTP 错误/传输错误/无效 JSON/异步入口）, covered indirectly through segmented/project integration where applicable | `_call_api_sync` 已 httpx 化（原 urllib），行为不变。B-P1-8b: `response_model=TTSResultOut` on /preset /voicedesign /voiceclone /voiceclone-direct. |
| 部署目标路由分流 | `main.py` (`create_app(deploy_target)`), `app/api/tts.py` / `app/api/clone.py` (`local_router` + lazy SDK import wrappers) | `tests/test_app_factory.py` | workers 模式不挂载 `/api/tts/batch`、`/api/clone/create-clone`、`/api/clone/create-clone-voxcpm`、`/api/clone/list-from-qwen`、`/api/clone/sync-from-qwen`；local_router 必须先于通用 router 注册（`/{voice_id}` 路径遮蔽陷阱）；子进程 sys.modules 审计 edge_tts/dashscope/qiniu 不泄漏。 |
| VoxCPM TTS | `app/api/voxcpm.py`, VoxCPM service code | Covered by API-level behavior where applicable | Local model/hardware paths should stay out of default pytest unless fully mocked. |
| Speech-to-text | `app/api/speech_to_text.py`, `app/services/voice_to_srt_service.py`, `app/services/funasr_service.py` | `tests/test_api_speech_to_text.py`, `tests/test_funasr_service.py`, `tests/test_stt_engines_fix.py` | Frontend storage returns content without history rows; backend storage persists history. `test_stt_engines_fix.py` locks two regressions: whisper `_download_model` allow_patterns 必须含 vocabulary.json/.txt（否则 ctranslate2 报 Cannot load the vocabulary，仅 5 月缓存的 large-v3 幸免）；FunASR 离线转写不提供 paraformer-zh-streaming（流式模型离线输出 00:00:00 全零时间戳+错乱文本）。 |
| FunASR service | `app/services/funasr_service.py` | `tests/test_funasr_service.py` | Real/manual FunASR checks live under `tests/manual/`. |
| Text split and SSML annotation | `app/api/text_split.py`, `app/services/text_split_service.py` | `tests/test_text_split_api.py`, `tests/test_text_split_service.py` | LLM calls are mocked in automated tests. |
| LLM client | `app/services/llm_client.py` | `tests/test_llm_client.py` | Covers config fallback, JSON extraction, structured calls, and validation retries. |
| Markdown chapter split | Markdown split API/service code | `tests/test_markdown_split_api.py` | Covers heading detection, slicing, front matter, and chapter merge behavior. |
| Segmented project assets | `app/core/segmented_assets.py` | `tests/test_segmented_assets.py` | Filesystem layout, manifest, text/SSML files, and cleanup. |
| Segmented project CRUD/service | `app/api/segmented_projects.py`, `app/services/segmented_project_service.py` | `tests/test_segmented_projects_api.py`, `tests/test_segmented_projects_service.py`, `tests/integration/test_chapters_batch.py` | Project/chapter/segment persistence, orphan cleanup (DB-path-first file deletion, including audio dropped from a kept segment's `audio` state), project-rename asset relocation (dir move + path rewrite), voice_ref save/load and auto-migration. `chapters:batch` replace-style rebuild including per-chapter `narration_script` and per-chapter `engine` (written into `chapter.voice`); top-level `narration_script` written to file with DB storing `narration_document_path` only (same path-based storage for `source_document`). |
| Segmented synthesis | `app/services/segmented_project_service.py`, `app/api/tts.py` bridge | `tests/test_segmented_synthesis.py`, `tests/test_synthesize_speech_internal.py` | Guards real synthesis bridge behavior and generated audio metadata. Also covers style-tag adaptation: text passed to the engine is cleaned/annotated via `prepare_text_for_engine` per engine/mode/`mute_tags`. |
| Recorded segment audio (self-recording) | `app/services/segmented_project_service.py` (`save_recorded_segment_audio`, `synthesize_segment` force/origin guard), `app/api/segmented_projects.py` (upload endpoint) | `tests/test_segment_recorded_audio.py` | Upload/transcode to unique `*.rec-*` file, `origin: 'recorded'` lock, demote-to-`previous` undo, orphan file cleanup, synthesize skip unless `force=True`, API 404/422 contracts. |
| Export all chapters (audio+SRT) | `app/services/segmented_project_service.py` (`export_all_chapters`, `resolve_export_target_dir`, `ChaptersIncompleteError`), `app/api/segmented_projects.py` (export-all-chapters endpoint) | `tests/test_export_all_chapters.py` | Dir resolution (absolute wins / remotion-relative / unconfigured 409), pre-check aborts whole export with incomplete chapter list (nothing written), per-chapter `{title}.mp3` + `{title}.srt` (chapter-local timeline from 0), API 200/404/409 contracts. 409 `chapters_incomplete` detail 现含 `missing_counts`（每章缺音频分片数）。 |
| Style tag engine adaptation | `app/services/engine_capabilities.py`, `app/services/srt_service.py` | `tests/unit/test_engine_capabilities.py`, `tests/test_srt_service.py` | Engine capability matrix, inline/leading tag strip, leading tag apply (idempotent), voxcpm mode caps (ultimate strips all), SRT subtitle tag cleaning. Mirrors `frontend/src/services/styleTags.ts`. |
| Silent/missing segment marking | Segment maintenance scripts/services | `tests/test_mark_silent_segments.py` | Ensures missing/silent audio is marked without deleting files. |
| Source library | `app/api/sources.py`, source models/services | `tests/test_sources_api.py`, `tests/test_narration_models.py` | Source documents are project-level data. |
| Narration documents | `app/api/narrations.py`, `app/models/narration.py` | `tests/test_narrations_api.py`, `tests/test_narration_models.py` | Versioned project-level narration documents and chapter slice references. |
| Animation specs | Segmented project animation fields/API | `tests/test_animation_spec_api.py` | Animation spec persistence and batch apply behavior. |
| Audio encoding | `app/core/audio_encoder.py` | `tests/test_audio_encoder.py` | ffmpeg transcode, duration probe, and invalid input behavior. |
| Config crypto | Config encryption helpers | `tests/test_config_crypto.py` | Fernet/RSA encryption and encrypted value format. |
| System config (storage mode + animation root) | `app/core/system_config_service.py`, `app/api/config.py` | `tests/unit/test_system_config_service.py`, `tests/test_config_animation_root_api.py` | `animation_root_folder` get/set round-trip, path validation (creatable/writable), and GET/PUT/test endpoints. |
| Narration git versioning + push | `app/services/narration_versioning/` (job/git_ops/scheduler), `app/api/config.py` (narration-git-* endpoints) | `tests/services/narration_versioning/test_git_ops.py`, `tests/services/narration_versioning/test_job.py`, `tests/test_narration_git_api.py`, E2E `tests/e2e/specs/narration-git-push.spec.ts` | Daily cron snapshot (serialize+commit); `git_ops.push` (set-url/add + push, no force); `snapshot_all(remote_url=...)` push + `push_error` capture; `narration_git_remote` DB setting; manual snapshot endpoint. Frontend: `src/components/Settings/NarrationGitSetting.test.tsx`. |
| Remotion scaffold | `app/services/remotion_scaffold_service.py`, `app/api/segmented_projects.py` (scaffold-remotion) | `tests/test_remotion_scaffold.py` | Target resolution hierarchy (`target_dir` > `remotion_project_path` > global setting), `safe_project_dirname`, create/refresh idempotency, 422 `animation_root_not_configured`. Agent node: `agent/tests/test_kv_scaffold.py` (forwards override only, 422 guidance). Frontend: `src/components/Settings/AnimationRootSetting.test.tsx`. |
| Project export/import | `app/services/project_export_service.py`, `app/services/project_import_service.py`, `app/api/segmented_projects.py` (export/import) | `tests/test_project_export_service.py`, `tests/test_project_import_service.py`, `tests/test_project_export_import_api.py`, E2E `tests/e2e/specs/project-export-import.spec.ts` | Export bundles DB rows + assets (segment audio, voice audio, docs) into ZIP with manifest; import remaps IDs (FK rewrite), writes files, resets `remotion_project_path`. Guard: `project_assets_not_under_project_dir`. Frontend: `src/components/ProjectHub/ProjectHub.test.tsx` (export/import callbacks). |
| Narration layer-sync (Phase A + B) | `app/services/layer_sync_service.py`, `app/services/segmented_project_service.py` (`batch_create_structure` hook, `resplit_from_script`), `app/api/segmented_projects.py` (split hook + `GET /sync-status` + 2 sync-action endpoints) | `tests/test_layer_sync_service.py`, `tests/test_layer_sync_integration.py`, E2E `tests/e2e/specs/layer-sync.spec.ts` | Phase A: `chapter.sync_state` JSON (l1/l2/segments hashes); `mark_consistent` at batch_create, `mark_split` at split, generic save_project untouched; `sync_status` dirty detection. Phase B: `segment.split_anchor` JSON (offset_start/offset_end/baseline_text, migration P16); `mark_split` writes anchors; `POST /resplit-from-script` (rule_split from L2, old segment config discarded, re-baseline); `POST /rewrite-script-from-segments` (localisation merge L3->L2, 409 `l2_dirty_conflict`). Frontend: `src/components/SegmentedTTS/ChapterSyncBadges.test.tsx`, `src/components/SegmentedTTS/ChapterSyncModal.test.tsx`. |

## Manual Tests

Manual scripts are kept under `tests/manual/` and intentionally do not run in the default pytest suite. They are for local diagnostics, real model checks, or one-off service verification.

Rules:

- Use the `manual_*.py` prefix.
- Do not use the `test_*.py` prefix.
- Do not rely on manual scripts for CI/default automated coverage.
- If a manual script becomes stable and important, convert it into a mocked automated test under `tests/`, `tests/unit/`, or `tests/integration/`.
| Unified data root + asset naming (plan B) | `app/core/segmented_assets.py`, `app/core/config.py` (`data/projects`), `app/services/data_root_migration.py`, `scripts/migrate_to_data_root.py` | `tests/test_segmented_assets.py`, `tests/test_data_root_migration.py` | Project dir = name slug (+hash suffix on collision via manifest); chapter/segment paths = raw DB ids; readers always use DB-stored paths; migration plan/apply (dry-run default), idempotent, multi-chapter short-id collision coverage. |
| Unified storage (voices/tts-history/srt, PR-B) | `app/core/config.py` (data/voices|tts-history|srt|temp), `app/api/clone.py`, `app/api/tts.py`, `app/api/mimo_tts.py`, `app/api/voxcpm.py`, `app/services/qwen_tts_service.py`, `app/services/unified_storage_migration.py`, `scripts/migrate_to_unified_storage.py` | `tests/test_unified_storage_migration.py` | Clone samples -> data/voices/profiles, previews -> data/voices/previews, TTS history -> data/tts-history, srt -> data/srt; reads are DB-path driven with legacy-dir fallback; migration plan/apply, idempotent, rewrites voice_params/preview/audio_path. |
| Markdown chapter split (按标题拆分章节) | `app/api/text_split.py` (`markdown-detect`/`markdown-split`), `app/api/segmented_projects.py` (`chapters:batch`) | `tests/test_markdown_split_api.py`, frontend `src/components/ProjectLibrary/ChapterSplitModal.test.tsx`, E2E `tests/e2e/specs/chapter-split.spec.ts` | 旁白文档 tab 章节列表顶栏 + 全文视图顶栏「按标题拆分章节」：detect 层级 → 选 levels → split 预览 → chapters:batch 替换章节（含替换警告 + layer-sync 基线验证）。 |
| 手动导入旁白文档 + 全文视图 | `frontend/src/components/ProjectLibrary/NarrationDocView.tsx`, `frontend/src/hooks/useSegmentedProject.ts` (`SET_NARRATION_SCRIPT`), `backend/app/api/segmented_projects.py` (`PUT` `narration_script` -> `narration_document_path`; `chapters:batch` `original_text` + 旧章节音频清理), `backend/app/services/segmented_project_service.py` (`batch_create_structure`, `_delete_segment_audio_files`) | frontend `src/components/ProjectLibrary/NarrationDocView.test.tsx`, `src/components/ProjectLibrary/ProjectLibrary.test.tsx` (fulltext form A/B + modal regression), `src/components/ProjectLibrary/ChapterSplitModal.test.tsx` (正文剩标题 + 确认弹窗), `src/hooks/__tests__/useSegmentedProject.test.ts` (`SET_NARRATION_SCRIPT`), backend `tests/integration/test_chapters_batch.py` (`test_batch_persists_chapter_original_text`, `test_batch_deletes_existing_chapter_audio`, `test_batch_chapter_has_default_split_config`), frontend `src/components/SegmentedTTS/TextInputPanel.test.tsx` (空 split_config 不崩), E2E `tests/e2e/specs/narration-manual-import-split.spec.ts` | 全文视图自适应：`narration_script` 空 = 章节合并只读预览 + 转换入口（粘贴旁白文档 / 从现有章节生成）；非空 = 编辑/预览切换 + 拆分按钮。拆分剩标题行得到正文，`original_text`+`narration_script` 都落正文（章节卡片有内容、studio 可拆）；章节名默认带 `01.` 零填充序号前缀；`create_chapter_for_project` 设默认 `split_config`（delimiters=除“、”外的全选 + mode），batch 建的章节进 studio 规则拆分不崩；已有章节时点应用弹确认（alertdialog）再替换，替换时清理旧章节音频文件。后端零接口改动（复用 `ProjectIn.narration_script`）。 |
| Post-synthesis audio adjust (tempo/volume) | `app/core/audio_encoder.py` (`adjust_audio_speed_volume`), `app/services/segmented_project_service.py` (`adjust_chapter_audio`, `synthesize_segment`, `update_segment_after_synth`, `save_project`), `app/api/segmented_projects.py` (`adjust-audio`) | `tests/test_audio_encoder.py`, `tests/test_adjust_audio.py` (含 D1/D2 `test_adjust_skips_recorded_segments`、D3 `test_synthesize_fails_when_chapter_adjust_fails`、D5 `test_adjust_probe_failure_aborts_without_clobbering_duration` / `test_synthesize_probe_failure_after_adjust_falls_back_to_estimate`、D6 `test_save_project_ignores_payload_audio_adjust`、D7 顶层 `duration_sec` 断言）, frontend `src/components/TTSSynthesis/AdjustAudioDialog.test.tsx`, E2E `tests/e2e/specs/studio-adjust-audio.spec.ts`（含 D1/D2 录音段豁免变速、D6/D7 插入段继承变速 + SRT 时间轴回归）、E2E `tests/e2e/specs/studio-batch-export.spec.ts`（导出 SRT 跳过未 ready 段） | ffmpeg atempo(0.5–2.0) + volume(-12~+12dB) 章节级批处理；绝对语义（始终从原始渲染，chapter.audio_adjust 记录已应用参数，恒等即还原）；时长重新 probe；P17 migration。录音段（origin=recorded）豁免变速/还原（响应含 `skipped_recorded`）；合成路径变速失败即整段合成失败；adjust 路径 probe 失败经 SAVEPOINT 中止报错，synthesize 路径回退「变速前时长÷tempo」估算；`audio_adjust` 只能由 adjust-audio 端点写（`save_project` 忽略 payload 值）；合成/录音后顶层 `duration_sec` 与 `current.duration_sec` 同步。 |
| Chapter & segment reorder | `frontend/src/hooks/useSegmentedProject.ts` (`MOVE_CHAPTER` action, `REORDER` renumbers `position`), `frontend/src/components/ProjectShell/ProjectShell.tsx` (chapter ↑/↓), `frontend/src/components/SegmentedTTS/SegmentRow.tsx` + `SegmentList.tsx` (segment ↑/↓ -> `onReorder`), `frontend/src/pages/TTSSynthesis.tsx` (wiring), `backend/app/services/segmented_project_service.py` (`save_project` reconciles `position` from payload, two-phase sentinel update for swap safety) | frontend `src/hooks/__tests__/useSegmentedProject.test.ts` (`MOVE_CHAPTER` order+renumber, `REORDER` renumber, boundary no-op), `src/components/ProjectShell/ProjectShell.test.tsx` (chapter move buttons + boundaries + absent-when-no-callback), `src/components/SegmentedTTS/SegmentList.reorder.test.tsx` (segment move buttons -> `onReorder(from,to)` + boundaries + single-segment hidden), E2E `tests/e2e/specs/reorder.spec.ts`, backend `tests/test_segmented_projects_service.py` (D6: duplicate position rejection × 2) | 章节侧边栏与段落行各加 ↑/↓ 按钮；前端 reducer 重排数组后按索引重写 `position`（后端 `save_project` 信任 `position`，不重写会被旧值静默回滚，故必须重编号）；乐观更新 + 防抖自动保存 PUT 全量项目；E2E 双读校验 UI→API→DB `position` 三层一致并在用例末还原顺序。D6: `UniqueConstraint(project_id, position)` + `UniqueConstraint(chapter_id, position)` 防止重复 position；`save_project` 用两阶段更新（先置负哨兵值再写最终值）支持 swap 重排；P9007 迁移去重已有重复数据。 |
| Role delete confirmation | `frontend/src/components/SegmentedTTS/RoleLibraryPanel.tsx` (global delete), `frontend/src/pages/TTSSynthesis.tsx` (`handleDeleteRole` project-level removal) | `frontend/src/components/SegmentedTTS/RoleLibraryPanel.deleteConfirm.test.tsx`, E2E `tests/e2e/specs/role-delete-confirm.spec.ts` | 两个删除入口均有二次确认，文案说明已合成音频保留、仅清角色引用；项目内移除不全局删除。 |
| Unified Toast/Confirm feedback channel (F-P1-4) | `frontend/src/components/ui/` (`toastContext`/`useToast`/`Toast` + `confirmContext`/`useConfirm`/`Confirm`, 3-file split per `react-refresh` rule), `App.tsx` (providers at root), 16 `alert()` -> `useToast().error` (App/TextInputPanel/MultiAudioSelector/VoiceList/AudioUploader/AudioRecorder/ProjectShell/ProjectLibrary/WorkflowDrawer), 6 `confirm()` -> `await useConfirm()` (App/ExportDialog/RoleLibraryPanel/SegmentEditDrawer/ProjectShell/WorkflowDrawer); removed hand-rolled toasts in `TTSSynthesis`/`ModelConfig`; `--z-toast` token | `frontend/src/components/ui/Toast.test.tsx` (5: render/aria-live/auto-dismiss/stack/timer-cleanup), `frontend/src/components/ui/Confirm.test.tsx` (4: resolve true/cancel/overlay/empty-by-default), updated `App.test.tsx`/`ProjectShell.test.tsx`/`RoleLibraryPanel.deleteConfirm.test.tsx` (assert `role=alert`/`role=alertdialog` instead of `window.alert`/`window.confirm`), E2E `project-crud.spec.ts` + `layer-sync.spec.ts` (migrated off `page.on('dialog')` to clicking `role=alertdialog`) | 统一反馈通道：Toast（`role=status`/`role=alert` + `aria-live`，每条独立 timer、unmount 清理，修复旧实现“快速连发互清 + unmount 后 setState”两个 bug）+ `useConfirm`（`Promise<boolean>` 命令式 API，背后是 `ConfirmDialog`）。Hook 在无 provider 时 no-op 回退（对齐 `useTranslation` 约定）。E2E 不再用 `page.on('dialog')`。 |
| Structured error responses (A8) | `backend/main.py` (custom `HTTPException` handler: wraps all HTTPException in `{code, message}`); frontend `getErrorDetail` helpers updated in `VoiceList`/`UrlInput`/`TextInputPanel`/`VoxCPMPanel`/`useTranscription`/`AnimationRootSetting`/`App.tsx` (7 files) | `tests/test_main_error_contract.py` (4: machine code / sentence / passthrough / health unaffected); 5 existing test files updated (`detail` -> `code`/`message`); frontend 357✓, e2e 45✓ | 所有 HTTPException 响应统一为 `{code, message}`：机器码(snake_case)同时作为 code 和 message；非机器码(句子/异常)用 `http_{status}` 作 code，原文作 message。前端 `getErrorDetail` 支持读取对象格式。为后续各端点使用稳定错误码做铺垫。 |
| voice_id 重命名为 profile_id (A9) | `app/api/clone.py` (`RegisterRequest.voice_id` → `profile_id`), `app/api/mimo_tts.py` (`MiMoVoiceCloneRequest`), `app/api/voxcpm.py` (`VoxCPMCloneRequest`, `VoxCPMUltimateCloneRequest`); frontend `api.ts` (`createClone`/`createCloneMiMo`/`createCloneVoxCPM`) | `tests/integration/test_clone_api.py` (27 clone tests); backend 536✓, frontend 364✓, e2e 46✓ | `voice_id` 在三个上下文含义不同（Qwen 云端 id / 本地 VoiceProfile.id / edge voice name）。clone/mimo/voxcpm 请求 schema 的 `voice_id` 重命名为 `profile_id`（本地数据库主键）。`TTSRequest.voice_id`（CosyVoice 云端 ID）、`TTSResultRecord.voice_id`（上下文相关）、`EngineParams.voice_id`（通用）不变。URL 路径 `/{voice_id}` 不变。 |
| Studio batch ops (menu synthesize + batch delete) | `frontend/src/pages/TTSSynthesis.tsx` (`handleRegenerateAll(mode)`, selection state, `handleDeleteSelected`), `frontend/src/components/SegmentedTTS/BatchSynthesizeMenu.tsx`, `SegmentRow.tsx`/`SegmentList.tsx` (selection props), `frontend/src/hooks/useSegmentedProject.ts` (`DELETE_SEGMENTS`) | frontend `src/hooks/__tests__/useSegmentedProject.test.ts` (batch delete + selection cleanup), `src/components/SegmentedTTS/BatchSynthesizeMenu.test.tsx`, `src/components/SegmentedTTS/SegmentRow.selection.test.tsx`, E2E `tests/e2e/specs/studio-batch-selection.spec.ts` | 批量合成改为下拉菜单：「仅合成未合成」只动 idle/failed，「重新合成全部」为原行为；recorded 段始终跳过。「选择」模式提供行首 checkbox（紧凑+展开）、全选、删除选中（确认弹窗含音频警示），删除走单次 `DELETE_SEGMENTS` dispatch；backend 模式文件清理由 save_project reconcile 兜底。 |
| 一键制作全本 + 修复「假 ready」 | `backend/app/services/segmented_project_service.py` (`get_project_detail`→`_audio_with_file_exists`、`ChaptersIncompleteError.missing_counts`), `backend/app/api/segmented_projects.py` (export-all 409 体), `frontend/src/hooks/useSegmentedProject.ts` (`enrichSegment` file_exists 门控), `frontend/src/services/produceAll.ts` (`chaptersNeedingSplit`、`selectProduceAllSegments`), `frontend/src/pages/TTSSynthesis.tsx` (`handleProduceAll`、`handleRegenerate` 重构为全项目查段), `frontend/src/components/VoiceStudio/VoiceStudioLayout.tsx` + `SegmentedTTS/BatchSynthesizeMenu.tsx` (transport bar 下拉) | backend `tests/test_segment_audio_file_exists.py`、`tests/test_chapter_split_empty.py`、`tests/test_export_all_chapters.py` (missing_counts); frontend `src/services/produceAll.test.ts`、`src/hooks/useSegmentedProject.test.ts` (file_exists 门控)、`src/components/VoiceStudio/VoiceStudioLayout.test.tsx`; E2E `tests/e2e/specs/produce-all.spec.ts` (edge-tts) | transport bar「一键制作全本」下拉（增量/覆盖）：先给无 segment 的章节按规则补切（复用 chapter 音色、非破坏），再全本顺序合成（复用每段已有音色、跳过 recorded）。修复导出全部「假未完成」：后端给 `audio.current.file_exists`（stat），前端 ready 需 `path && file_exists!==false`，文件丢失段降 idle，导出校验保持严格、409 报错加 `missing_counts`。实现中修复：`/chapters/{cid}/split` 全量 reconcile 丢 `configs` 等（补齐）；produce-all 合成循环与 autosave 竞态会覆盖刚合成段音频（循环前暂停 autosave，后 reload 恢复）。 |
