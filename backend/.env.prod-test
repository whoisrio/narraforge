# Production-DB test overlay — uses the real voice_clone.db instead of voice_clone_e2e.db.
# Backend loads .env first, then this file on top.
# APP_ENV=e2e is required so global-setup.ts health check passes.
APP_ENV=e2e
DATABASE_URL=sqlite:///./voice_clone.db
